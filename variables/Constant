#include <stdio.h>
#include <math.h>

int main() {
    // Define your PI constant here
    #define PI 3.14159
    int radius = 5;
    double area;

    // Calculate the area here
    area= PI * radius * radius;

    // Print the area here
    printf("Area: %.2f\n", area);
    return 0;
}